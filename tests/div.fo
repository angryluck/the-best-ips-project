fun int div() =
  let n = read(int) in
  let d = read(int) in
  write(n/d)

fun int main() =
  let tmp = div() in
  let tmp = div() in
  let tmp = div() in
  let tmp = div() in
  let tmp = div() in
  let tmp = div() in
  let tmp = div() in
  div()
