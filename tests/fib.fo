fun int fibo(int n) =
  if n == 0 then 0
  else if n == 1 then 1
  else fibo(n - 1) + fibo(n - 2)

fun int fib_case() =
  let n = read(int) in
  let r = fibo(n) in
  write(r)

fun int main() =
  let tmp = fib_case() in
  let tmp = fib_case() in
  let tmp = fib_case() in
  let tmp = fib_case() in
  let tmp = fib_case() in
  let tmp = fib_case() in
  let tmp = fib_case() in
  fib_case()